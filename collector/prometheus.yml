global:
  scrape_interval: 10s

scrape_configs:
  # The OTel Collector re-exposes everything it receives on :8889.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
